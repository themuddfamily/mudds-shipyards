extends Node

## One stream is deliberately retained only in script state so the focused
## census proves it inventories reachable resources, not only bound players.
var retained_streams: Dictionary = {}
